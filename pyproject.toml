[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ads-metrics-core"
version = "0.1.0"
description = "Pure domain logic for calculating ads metrics"

[tool.setuptools]
packages = ["packages", "packages.core"]